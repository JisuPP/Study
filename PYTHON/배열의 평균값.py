def solution(numbers):
    lenN = len(numbers)
    totalN = sum(numbers)
    return totalN/lenN

def solution(numbers):
    return sum(numbers) / len(numbers)

import numpy as np
def solution(numbers):
    return np.mean(numbers)